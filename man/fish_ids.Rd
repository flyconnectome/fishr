% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ids.R
\name{fish_ids}
\alias{fish_ids}
\title{Resolve fish2 body ids from a variety of inputs}
\usage{
fish_ids(
  x,
  mustWork = TRUE,
  as_character = TRUE,
  integer64 = FALSE,
  unique = TRUE,
  conn = NULL
)
}
\arguments{
\item{x}{A numeric/character vector of body ids, a \code{data.frame} with a
\code{bodyid} column, or a neuprint query string (e.g. \code{"DNa02"},
\code{"where:status='Traced'"}).}

\item{mustWork}{If \code{TRUE} (default) an error is raised when no ids are
found.}

\item{as_character}{Whether to return ids as character (default \code{TRUE}).
Character is the safest default because body ids may exceed the 53-bit
integer limit of double-precision floating point (\code{2^53 - 1}).}

\item{integer64}{Whether to return ids as \code{bit64::integer64}
(default \code{FALSE}). Takes precedence over \code{as_character}.}

\item{unique}{Whether to de-duplicate the result (default \code{TRUE}).}

\item{conn}{A \code{neuprint_connection} object (default \code{NULL} uses
\code{\link{fish_neuprint}}).}
}
\value{
A character vector of body ids (default), or
  \code{bit64::integer64} when \code{integer64=TRUE} or
  \code{as_character=FALSE}.
}
\description{
Extracts body ids from vectors, data.frames, or query strings.
  Query strings are passed to \code{malevnc::\link[malevnc]{manc_ids}} via
  the fish2 neuprint server.  For filtering DVID annotations by field, see
  \code{\link{fish_dvid_annotations}}.
}
\examples{
\dontrun{
# neuprint query by type
fish_ids("DNa02")
# neuprint where clause
fish_ids("where:status='Traced'")
# numeric ids passed through
fish_ids(c(12345, 67890))
# from a data.frame
df <- fish_dvid_annotations()
fish_ids(df)
}
}
\seealso{
Other fishr-package: 
\code{\link{fish_dvid_annotations}()},
\code{\link{fish_setup}()},
\code{\link{read_fish_meshes}()},
\code{\link{with_fish}()}
}
\concept{fishr-package}
